# pointage-odoo
Script de pointage pour Odoo utilisant l'API externe.  
Le script est développé pour la version 9 de Odoo. Il est donc probable qu'il ne fonctionne pas sur les versions les plus récentes.  

Utilisez le script `/build_script.sh` pour générer un exécutable pour linux/macos.
